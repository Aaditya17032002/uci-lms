import { ApprovedTimesheetsPage } from "../../modules/approved-timesheets"

interface ApprovedTimesheetsProps {
  isDarkMode: boolean
}

export default function ApprovedTimesheets({ isDarkMode }: ApprovedTimesheetsProps) {
  return <ApprovedTimesheetsPage />
}
